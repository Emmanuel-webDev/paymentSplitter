# Payment Splitter

## Description

Smart contract that allows users to deposit stables(USDC for now), then allows a user send multiple stablecoin transactions at once to different addresses based on pre-defined percents each address is to receive. 
